function setup() {
    // happens only once at page load
    createCanvas(600, 600)
    background(127)

}

function draw() {
    // position x axis, y axis, size

    fill(0, 255, 0)
    stroke(255, 0, 255)
    strokeWeight(10)
    square(150, 50, 100)



}